import { FilterSoundsPipe } from './filter-sounds.pipe';

describe('FilterSoundsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSoundsPipe();
    expect(pipe).toBeTruthy();
  });
});
